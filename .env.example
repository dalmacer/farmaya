# FarmaYa — Variables de entorno
# Copiá este archivo como .env y completá los valores reales.
# NUNCA subas el .env real (con el token) a un repo público.

# Token del bot de Telegram, generado con @BotFather
TELEGRAM_TOKEN=7xxxxxxxxx:AAxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Puerto donde corre el backend Node dentro del contenedor
PORT=3000
